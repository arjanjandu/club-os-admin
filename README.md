# Club OS - Admin Portal 🌩️

**White-label Backend & CRM for Tramp Health.**

## Architecture 🏗️

-   **Frontend:** Next.js (Admin Dashboard) -> Deploys to Azure Static Web Apps.
-   **Backend:** Node.js/Express API -> Deploys to Azure App Service (Linux B1).
-   **Database:** Azure SQL (Serverless/S0) -> Managed via Sequelize ORM.
-   **Infrastructure:** Azure Bicep (`infra/main.bicep`).

## Directory Structure 📂

-   `frontend/`: Next.js App Router (Dashboard, Members, Products, Content, Invoices).
-   `backend/`: Express API + Sequelize Models.
-   `infra/`: Azure Resource Manager (Bicep) templates.
-   `deploy.sh`: Deployment automation script.

## Getting Started 🚀

### Prerequisites
-   Node.js 20+
-   Azure CLI (`az login`)
-   GitHub CLI (`gh auth login`)

### Backend Setup
1.  `cd backend`
2.  `npm install`
3.  Create `.env` with `SQL_CONNECTION_STRING` (from Azure output).
4.  `node server.js`

### Frontend Setup
1.  `cd frontend`
2.  `npm install`
3.  Create `.env.local` with `NEXT_PUBLIC_API_URL=http://localhost:8080/api` (or Azure URL).
4.  `npm run dev`

## Deployment ☁️

Run the automated script:
```bash
./deploy.sh
```
This will:
1.  Create/Update GitHub Repo.
2.  Provision Azure Resources.
3.  Deploy Backend API via Zip Deploy.
4.  Output URLs for Frontend configuration.

## Credentials 🔑

-   **Admin User:** (See Azure App Service Configuration / SQL Server).
-   **Test Login:** (Currently open/mock for MVP).

## Security 🔒

-   **GDPR:** Data minimization principles applied.
-   **Network:** HTTPS Only, TLS 1.2 enforced.
-   **Secrets:** Managed via App Service Application Settings (Environment Variables).

---
*Generated by Lowki (OpenClaw Agent) for Operation Club-OS.*
